# APEX v0.6 acceptance and handoff — 2026-09-16

The connected research vertical is implemented and verified locally. It includes
an AI proposal boundary, an original peer-dislocation hypothesis, matched
information tests, reconstructed outcomes and a deterministic research verdict.
It establishes neither a profitable edge nor a live autonomous trading system.

## Accepted work

- Retention repair `09cdad7` binds successful completion to the writer's actual
  terminal ledger head, including the lifecycle accounting reader. The recovered
  v0.5 SPY study verified completely and selected WAIT in all 319 tournaments.
- v0.6 adds the market/sector/stock twin and four-model matched experiment. The
  source bytes used by acceptance are retained in `tested-source-manifest.json`.
- The director validates a bounded proposal, runs and verifies both research
  children, preserves provenance and emits an outcome-derived next action.
- The actual market run uses the retained Codex-authored external proposal.
  Runtime does not authenticate that authorship and made no API-model call.
- The completed full suite has **392 passed, zero failures, errors or skips**.
  `acceptance.json` records the JUnit digest and executed/unexecuted gates.
- The real lifecycle demo and verifier passed. The recorded-data director's
  separate CLI verifier returned **VALID**, six verified stages and matching
  current source. Both synthetic director CLI controls completed successfully.

## What the market study found

The fixed AAPL/SPY/XLK acquisition returned 33,538 minute bars for August 3–21,
2026. Each instrument has 5,850 observed regular-session bars over 15 dates.
The plan uses five warmup, five development and five held-out sessions. Inputs
retain bar event time and assumed completion availability separately from
retrieval; they do not contain original receipts or contemporaneous quotes.

There were 60 forecasts, 90 matured source outcomes and 20 matched held-out
forecast comparisons. One setup was eligible. Every experimental model chose
WAIT; no experimental opportunity passed its modeled after-cost admission.
Peer-minus-own-stock CRPS was +4.2095359428661227e-07, a small deterioration.
There was no incremental policy-economic value. The director's conclusion was
**HYPOTHESIS_NOT_SUPPORTED** and its next action was to reject or revise on new
data. No trading edge, significance, capacity or growth rate was established.

## Controls and interpretation

| Retained study | Held-out result | Director assessment |
|---|---|---|
| Recorded August market | All policies WAIT; no incremental peer value | Hypothesis not supported |
| Planted synthetic catch-up | Positive counterfactual policy economics, identical for peer and own-stock | Hypothesis not supported |
| Synthetic continuation after development catch-up | Negative counterfactual held-out economics; no incremental peer value | Hypothesis not supported |

The controls demonstrate that the implementation can expose gain and loss
outcomes and reject an unhelpful added feature. They do not validate predictive
success in markets. Synthetic opportunity economics are not account returns.

The experiment isolates the added lag-gap feature among policies that share
peer-derived setup eligibility. It does not isolate all peer information from
a wholly peer-free strategy. Three-times-cost diagnostics keep the original
decision fixed; they are not the original lab's seven-world admission rule.
Factor state updates causally during holdout; supervised coefficients and
scalers remain frozen. Five held-out sessions are exploratory evidence.

## Verification limits and outstanding work

Independent reader invocation reconstructs retained inputs, stages, policies,
forecasts and results. Readers share numerical functions; this is not an
independent mathematical implementation. Hashes bind internal content, not
provider authenticity, execution history or prospective registration.

The OpenAI planner has injected-transport and deadline tests, but live runtime
inference was not exercised because no runtime key/model was configured.
TradingView is not connected. Broad live scanning, global experiment accounting,
controlled promotion, live paper execution and position recovery remain
uncommissioned. No orders or fills were created by this director.

The next vertical is the timestamped opportunity queue described in
`../../ACTIVE_HUNTING_DIRECTIVE.md`. Preserve the failed peer hypothesis and
register any revision against new evidence before evaluating it.

## Repository handoff

Changes are local on `codex/peer-edge-ai-director-001`, based on the retained
completion repair. Raw licensed inputs and generated run trees remain in
ignored `data/` and `runs/`; only compact evidence and reproducible source are
intended for Git. The input acquisition is preserved separately.

Automatic approval review rejected the earlier GitHub push because uploading
the repository code/evidence to that destination lacked explicit authorization.
No workaround or later upload was attempted. GitHub Actions has not run against
these changes. Uploading the verified commits to `derekduenas/APEX` requires
the user's explicit approval; local completion and review proceeded unaffected.
